export interface EventsPageContent {
  hero: {
    title: string;

    description: string;
  };

  upcomingEvents: {
    title: string;
  };

  eventArchive: {
    title: string;
  };

  communityImpact: {
    title: string;

    ctaText: string;
  };
}
