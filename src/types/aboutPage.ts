export interface AboutPageContent {
  designerStory: {
    title: string;
  };

  brandStory: {
    title: string;
  };

  craftProcess: {
    title: string;
  };

  visionImpact: {
    title: string;
  };

  contactCta: {
    title: string;
    description: string;
    buttonText: string;
  };
}
